name: repolinter

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2.4.0 
    - name: Run a repolinter
      uses: philips-labs/github-action-repolinter@v1.0.0
